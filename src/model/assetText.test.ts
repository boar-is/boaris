import { Text } from '@uiw/react-codemirror'
import { describe, it } from 'vitest'

describe.concurrent('reversedTextChanges', () => {
  it('should return an empty array for no changes', () => {
    const initialValue = Text.of(qwe)
  })
})
